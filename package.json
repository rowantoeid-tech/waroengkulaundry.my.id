{
  "name": "waroengkulaundry",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo Waroengku Laundry — web, API, shared",
  "scripts": {
    "build": "npm run build --prefix apps/web",
    "build:web": "npm run build --prefix apps/web",
    "install:web": "npm install --prefix apps/web",
    "dev:web": "npm run dev --prefix apps/web",
    "preview:web": "npm run preview --prefix apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
